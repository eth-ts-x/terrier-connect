apiVersion: apps/v1
kind: Deployment
metadata:
  name: consumer-outbox-relay
spec:
  replicas: 1
  selector:
    matchLabels:
      app: consumer-outbox-relay
  template:
    metadata:
      labels:
        app: consumer-outbox-relay
    spec:
      serviceAccountName: server
      containers:
        - name: consumer
          image: server
          command: ["python", "manage.py", "run_outbox_relay", "--poll-interval", "1.0"]
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          env:
            - name: SKIP_STARTUP_INIT
              value: "1"
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets